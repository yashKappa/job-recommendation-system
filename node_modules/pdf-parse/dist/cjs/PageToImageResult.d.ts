import { InfoResult, Metadata } from './InfoResult.js';
export interface PageToImage {
    data: Uint8Array;
    dataUrl: string;
    pageNumber: number;
}
export declare class PageToImageResult implements InfoResult {
    pages: Array<PageToImage>;
    total: number;
    info?: object | undefined;
    metadata?: Metadata | undefined;
    constructor(info: InfoResult);
}
